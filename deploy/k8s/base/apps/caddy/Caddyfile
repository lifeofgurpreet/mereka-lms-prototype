# Global configuration
{



}

# proxy directive snippet (with logging) to be used as follows:
#
#     import proxy "containername:port"
(proxy) {
    log {
        output stdout
        format filter {
            wrap json
            fields {
                common_log delete
                request>headers delete
                resp_headers delete
                tls delete
            }
        }
    }

    # This will compress requests that matches the default criteria set by Caddy.
    # see https://caddyserver.com/docs/caddyfile/directives/encode
    # for information about the defaults; i.e. how/when this will be applied.
    encode gzip

    reverse_proxy {args.0} {
        header_up X-Forwarded-Port {http.request.header.X-Forwarded-Port}
        header_up X-Forwarded-Proto {http.request.header.X-Forwarded-Proto}
    }


}

# LMS - localhost and production/dev domains
# - localhost: local development
# - academyv2.mereka.io: main GKE environment
# - academyv2.mereka.dev: VPS kind cluster
# - skillourfuture.academy.mereka.io: Skill Our Future microsite
# - academy.biji-biji.com: Biji-Biji Academy microsite
localhost{$default_site_port}, preview.localhost{$default_site_port}, academyv2.mereka.io{$default_site_port}, academyv2.mereka.dev{$default_site_port}, preview.academyv2.mereka.io{$default_site_port}, preview.academyv2.mereka.dev{$default_site_port}, skillourfuture.academy.mereka.io{$default_site_port}, academy.biji-biji.com{$default_site_port} {
    @favicon_matcher {
        path_regexp ^/favicon.ico$
    }
    rewrite @favicon_matcher /theming/asset/images/favicon.ico

    # Limit profile image upload size
    handle_path /api/profile_images/*/*/upload {
        request_body {
            max_size 1MB
        }
    }

    import proxy "lms:8000"



    handle_path /* {
        request_body {
            max_size 4MB
        }
    }
}

# Studio/CMS - shared across all LMS sites per environment
# - studio.localhost: local development (serves all local LMS sites)
# - studio.academyv2.mereka.io: GKE Studio
# - studio.academyv2.mereka.dev: VPS kind Studio
# - studio.academy.biji-biji.com: Biji-Biji Studio
studio.localhost{$default_site_port}, studio.academyv2.mereka.io{$default_site_port}, studio.academyv2.mereka.dev{$default_site_port}, studio.academy.biji-biji.com{$default_site_port} {
    @favicon_matcher {
        path_regexp ^/favicon.ico$
    }
    rewrite @favicon_matcher /theming/asset/images/favicon.ico

    import proxy "cms:8000"



    handle_path /* {
        request_body {
            max_size 250MB
        }
    }
}

# Discovery Service (Course Catalog API)
discovery.localhost{$default_site_port}, discovery.academyv2.mereka.io{$default_site_port}, discovery.academyv2.mereka.dev{$default_site_port} {
    request_body {
        max_size 10MB
    }

    import proxy "discovery:8000"
}
# E-commerce Service
ecommerce.localhost{$default_site_port}, ecommerce.academyv2.mereka.io{$default_site_port}, ecommerce.academyv2.mereka.dev{$default_site_port} {
    request_body {
        max_size 10MB
    }
    import proxy "ecommerce:8000"
}
# MFE (Micro-Frontends) - localhost and production/dev
# - apps.localhost: local development
# - apps.academyv2.mereka.io: main GKE MFEs
# - apps.academyv2.mereka.dev: VPS kind MFEs
# - apps.academy.biji-biji.com: Biji-Biji Academy MFEs
apps.localhost{$default_site_port}, apps.academyv2.mereka.io{$default_site_port}, apps.academyv2.mereka.dev{$default_site_port}, apps.academy.biji-biji.com{$default_site_port} {
    request_body {
        max_size 2MB
    }
    import proxy "mfe:8002"
}
# Notes API
notes.localhost{$default_site_port}, notes.academyv2.mereka.io{$default_site_port}, notes.academyv2.mereka.dev{$default_site_port} {
    import proxy "notes:8000"
}

# Credentials Service (Certificate issuance)
credentials.academyv2.mereka.io{$default_site_port}, credentials.academyv2.mereka.dev{$default_site_port} {
    import proxy "credentials:8000"
}

# Xqueue (external graders, local only)
xqueue.localhost{$default_site_port} {
    import proxy "xqueue:8000"
}
