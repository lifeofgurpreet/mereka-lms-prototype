---
apiVersion: v1
kind: Service
metadata:
  name: cms
  labels:
    app.kubernetes.io/name: cms
spec:
  type: ClusterIP
  ports:
    - port: 8000
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: cms
