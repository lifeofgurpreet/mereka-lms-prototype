---
apiVersion: v1
kind: Service
metadata:
  name: preview-redirect
  labels:
    app.kubernetes.io/name: preview-redirect
    app.kubernetes.io/component: redirect-service
    app.kubernetes.io/part-of: mereka-lms
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: preview-redirect
