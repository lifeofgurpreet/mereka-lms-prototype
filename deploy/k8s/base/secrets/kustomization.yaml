apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ExternalSecrets pull from GCP Secret Manager (populated from Infisical)
  - external-secrets.yaml
  # Static secrets template (for reference/local dev only)
  # - openedx-secrets.yaml
