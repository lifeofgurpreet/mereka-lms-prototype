# GKE Module (stub)

Intended to create an Autopilot GKE cluster in the shared VPC. Configure to enable Workload Identity and Autopilot defaults during implementation.

> TODO: add `google_container_cluster` resource using Autopilot mode and wire outputs accordingly.
