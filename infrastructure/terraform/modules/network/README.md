# Network Module (stub)

Creates the foundational VPC, subnetwork, and secondary IP ranges required by the GKE Autopilot cluster.

> Implementation pending: populate `main.tf` with `google_compute_network`, `google_compute_subnetwork`, and secondary ranges, then replace the placeholder outputs in `outputs.tf`.
