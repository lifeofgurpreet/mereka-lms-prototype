{
  "name": "auth-failures-credentials",
  "description": "Count auth failures in credentials service logs.",
  "filter": "resource.type=\"k8s_container\" AND resource.labels.container_name=\"credentials\" AND (textPayload=~\"\\s(401|403)\\s\" OR jsonPayload.status>=400 OR jsonPayload.status_code>=400)"
}
