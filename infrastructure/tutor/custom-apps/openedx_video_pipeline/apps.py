from django.apps import AppConfig


class OpenedxVideoPipelineConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'openedx_video_pipeline'
    verbose_name = 'Video Pipeline — MCT Migration'
