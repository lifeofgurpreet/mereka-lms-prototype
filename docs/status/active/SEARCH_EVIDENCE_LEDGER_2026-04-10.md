# Search Evidence Ledger — 2026-04-10
_Audience: operators and agents · Status: active evidence log_

| Claim | Status tag | Proof | Owner | Action needed |
|---|---|---|---|---|
| Staging Meilisearch API-key authority was stale at source and did not match any valid live built-in key | `Runtime-verified` | direct `/keys` inspection with the live master key showed built-in hashes `c6a9fe73b52c` (Default Search) and `37ac321c8245` (Default Admin), while staging `openedx-secrets` and CMS pod `MEILISEARCH_API_KEY` both hashed to stale value `52869808ae04` | secret authority | keep API-key authority in Infisical aligned to a valid routine Meilisearch key |
| Staging `MEILISEARCH_MASTER_KEY` was valid while the routine API key was stale | `Runtime-verified` | from the CMS pod, `GET /health` with `MEILISEARCH_API_KEY` returned `200`, `GET /keys` with the same key returned `403 invalid_api_key`, and `GET /keys` with `MEILISEARCH_MASTER_KEY` returned `200` | secret authority | keep master key reserved for key management and inspection only |
| Staging Infisical source authority is `bbi-secrets-tk-fb` / `staging` and was updated successfully | `Runtime-verified` | `ClusterSecretStore/infisical-secret-store-staging` plus Universal Auth login against `https://secrets.mereka.io/api` identified project `6bfac905-9bb1-449e-8be8-f25f9634802b`; `infisical secrets set MEREKA_LMS_MEILISEARCH_API_KEY=... --env staging --path / --projectId 6bfac905-9bb1-449e-8be8-f25f9634802b` returned `set_code=0` | secret authority | keep staging secret changes source-driven through Infisical |
| Staging ESO refresh realized the repaired API key into `openedx-secrets` | `Runtime-verified` | `kubectl annotate externalsecret openedx-secrets -n stg-mereka-lms force-sync=... --overwrite` followed by `kubectl describe externalsecret ...` showed `secret updated`; staging `openedx-secrets` API fingerprint moved from `52869808ae04` to `37ac321c8245` | realization / ESO | keep using explicit force-refresh when proving secret-authority repairs |
| Staging CMS and CMS-worker now consume the repaired API key | `Runtime-verified` | `kubectl rollout restart -n stg-mereka-lms deploy/cms deploy/cms-worker` completed; new pod env fingerprints show `MEILISEARCH_API_KEY` hash `37ac321c8245` in both CMS and CMS-worker | realization | keep proving consumers are restarted after secret-authority changes |
| Staging Meilisearch is source-owned enough to hold the repaired data path | `Runtime-verified` | merged infra PR `#2656` realized `replicas: 1`; `kubectl get deploy meilisearch -n stg-mereka-lms -o json` shows `replicas=1, available=1, updated=1`; PVC `meilisearch` remains bound and pod data path is `/meili_data/data.ms` | overlay / realization | keep a stable staging Meilisearch lane for promotion proof |
| Staging full course reindex now succeeds end-to-end on Meilisearch | `Runtime-verified` | `kubectl create job -n stg-mereka-lms --from=cronjob/course-reindex course-reindex-manual` finished `Succeeded`; logs include `39 of 39 courses reindexed succesfully.` | runtime / app repo | preserve the non-interactive reindex contract and repaired key authority |
| Staging Meilisearch now holds the full learner browse corpus after reindex settles | `Runtime-verified` | authoritative post-run `/indexes/*/stats` checks showed `tutor_course_info = 39` and `tutor_courseware_content = 2815` | runtime | use these counts as the staging promotion proof baseline |
| Staging learner browse path now works with a real course key | `Runtime-verified` | `curl -I https://staging.academyv2.mereka.io/courses` returned `200`; `curl -I https://staging.academyv2.mereka.io/courses/course-v1:MEREKA+MCT44-EN+course/about` returned `200`; `curl -s https://staging.academyv2.mereka.io/api/courses/v1/courses/` returned real course JSON | runtime | promote this same proof sequence into production after search promotion |
| Production LMS is not using the base repo Meilisearch settings | `Runtime-verified` | `kubectl exec -n mereka-lms deploy/lms -- python manage.py lms shell -c "from django.conf import settings; print(settings.SEARCH_ENGINE, settings.FORUM_SEARCH_BACKEND, settings.COURSE_CATALOG_URL_ROOT)"` returned `search.elastic.ElasticSearchEngine`, `forum.search.es.ElasticsearchBackend`, `http://localhost:8008` | overlay / realization | reconcile patched LMS configmap with app-repo intent |
| Production CMS is now aligned to the Meilisearch engine | `Runtime-verified` | `kubectl exec -n mereka-lms deploy/cms -- python manage.py cms shell -c "from django.conf import settings; print(settings.SEARCH_ENGINE, settings.MEILISEARCH_ENABLED, settings.MEILISEARCH_URL, settings.ELASTIC_SEARCH_CONFIG)"` returned `search.meilisearch.MeilisearchEngine`, `True`, `http://meilisearch:7700`, and `[{\"host\": \"meilisearch\", \"port\": 7700}]` | app repo / realization | keep proving live CMS stays on the realized Meilisearch contract after syncs and restarts |
| Production LMS and CMS now both mount patched settings configmaps after a manual full sync | `Runtime-verified` | `kubectl get deploy -n mereka-lms lms -o json` and `kubectl get deploy -n mereka-lms cms -o json` both show `settings-lms` / `settings-cms` volumes resolving to `openedx-settings-*-patched-*` | realization | keep proving that the realized runtime still matches current GitOps desired state after future syncs |
| Production `course-reindex` failed because the job was interactive | `Runtime-verified` | `kubectl logs -n mereka-lms job/course-reindex-29595990 --all-containers=true` ends with `EOFError: EOF when reading a line` from `query_yes_no()` | app repo / realization | keep the job non-interactive |
| Reindex now gets through startup and reaches Meilisearch writes | `Runtime-verified` | direct `kubectl exec` run of `printf 'y\\n' | python manage.py cms reindex_course --all` in live `cms` reached document indexing and emitted Meilisearch tasks instead of failing on prompt, Atlas, or auth | runtime / app repo | keep using the non-interactive path for proof, then verify index population after bootstrap repair is realized |
| Production Elasticsearch is live but `course_info` is empty | `Runtime-verified` | `curl 'http://127.0.0.1:19200/_cat/indices?v'` via `kubectl port-forward -n mereka-lms svc/elasticsearch 19200:9200` shows `course_info docs.count 0` | runtime | re-run reindex after non-interactive fix and verify doc population |
| Discovery is live but currently empty | `Runtime-verified` | `kubectl exec -n mereka-lms deploy/discovery -- python manage.py shell -c "from course_discovery.apps.course_metadata.models import Course, CourseRun, Program; print(...)"` returned zero counts | runtime | decide whether to repopulate Discovery or contain it |
| Discovery `/api/v1/courses/` is auth-protected in production | `Runtime-verified` | `curl -i https://discovery.academyv2.mereka.io/api/v1/courses/` returned `401` with `www-authenticate: JWT realm="api"` | runtime | correct docs and avoid public-API assumptions |
| Discovery sync and clickhouse sync CronJobs now exist live with Kyverno-compliant security context | `Runtime-verified` | `kubectl get cronjob discovery-sync clickhouse-data-sync-daily -n mereka-lms -o json` now shows pod `runAsNonRoot` plus container `allowPrivilegeEscalation: false` for both jobs | realization | keep these vendored cron surfaces aligned with app intent or replace them with a stronger sync guard |
| `MEILISEARCH_MASTER_KEY` is newline-clean and accepted by the live Meilisearch server | `Runtime-verified` | Meilisearch server pod `MEILI_MASTER_KEY` trimmed fingerprint matches the live master-key authority; `/keys` requests with the master key succeed | secret authority | keep master key limited to API-key management and server bootstrap only |
| `MEILISEARCH_API_KEY` is now aligned to the live Default Admin API Key in staging | `Runtime-verified` | after the Infisical repair and ESO refresh, staging `openedx-secrets` and CMS pod env both hash to `37ac321c8245`, which matches the live built-in Default Admin key | secret authority | keep routine CMS/LMS Meilisearch writes on this API key, not the master key |
| Repo now has a static guard for the proved search/Mongo contract | `Repo-verified` | `scripts/qa/verify-search-runtime-contract.sh` checks newline trimming, SRV-safe Mongo config, ESO key presence, and non-interactive reindex wiring | app repo | wire into broader QA/gates if this topic stays active |
| GitOps source now has a prod CMS patched-settings realization path | `Repo-verified` | current `bbi-infrastructure` `main` renders `openedx-settings-cms-patched` for `cms`, `cms-worker`, `course-reindex`, and `tenant-isolation-nightly` | overlay / realization | keep the overlay/app boundary legible and guarded against future drift |
| Production app auto-sync is disabled in source, so realization currently depends on an explicit manual sync | `Repo-verified` | `argocd/applications/mereka-lms-prod.yaml` has no `syncPolicy.automated` block and relies on explicit sync operations | realization / Argo | decide whether this remains intentional bridge policy or should be removed after prod stabilizes |
| Production app is now fully `Synced` on infra revision `0aebdace...` after the manual full sync | `Runtime-verified` | `kubectl get application mereka-lms-prod -n argocd -o json` reports `sync=Synced`, `phase=Succeeded`, `revision=0aebdaceace1ba59057b3cdf4a7eb4f972dd3c41` | realization / Argo | keep reporting live, git, and deployed states separately because sync success did not fix the search path |
| Live `course-reindex` now mounts patched CMS settings and is non-interactive | `Runtime-verified` | `kubectl get cronjob course-reindex -n mereka-lms -o json` plus one-off/manual runs show `printf 'y\\n' | python manage.py cms reindex_course --all` and no `EOFError` prompt failure | app repo / realization | keep the runtime-aware, non-interactive command path in source and realized config |
| `FORUM_MONGODB_HOST` is overloaded as effective modulestore connectivity authority in prod | `Runtime-verified` | `kubectl exec -n mereka-lms deploy/cms -- python manage.py cms shell -c "from django.conf import settings; print(settings.MONGODB_HOST, settings.DOC_STORE_CONFIG['host'])"` matched the same credential-bearing Atlas SRV URI sourced from `FORUM_MONGODB_HOST` | secret authority / app repo | split forum-vs-modulestore secret purpose instead of reusing one URI contract |
| Live search backend split is now explicit: LMS remains Elasticsearch while CMS writes to Meilisearch | `Runtime-verified` | `kubectl exec -n mereka-lms deploy/lms -- python manage.py lms shell -c ...` still prints `search.elastic.ElasticSearchEngine`; equivalent CMS proof now prints `search.meilisearch.MeilisearchEngine` | app repo / realization | keep `/courses` on Elasticsearch until Meilisearch learner index population is proved |
| The PR #1497 title/body claims a single-key master-key model | `Repo-verified` | `gh pr view 1497 --json title,body` | app repo | supersede or amend with current proved key model |
| Earlier wrong-primary-key Meilisearch index creation was a real blocker, not folklore | `Runtime-verified` | before the repair, live `/indexes/tutor_course_info` and `/indexes/tutor_courseware_content` reported `primaryKey: null` and `/tasks` showed `invalid_document_id` failures for raw ids like `course-v1:...` and `block-v1:...` | app repo / runtime | keep the bootstrap repair in source so first write cannot recreate this malformed state |
| Staging is now past the wrong-primary-key blocker | `Runtime-verified` | successful full reindex plus settled index counts prove the repaired staging lane no longer fails on `invalid_document_id` | app repo / realization | promote the same repair sequence into production and re-prove there |
