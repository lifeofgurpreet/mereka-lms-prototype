# Lane P1 — Cold-Standby Truth + Governed Readiness

Date: 2026-03-15T21:10Z

## Active Env Starting State

| Item | Value |
|------|-------|
| mereka-lms main | `64db9562` (PR #926 merged) |
| bbi-infrastructure main | `1c838d61` (PRs #1824 + #1825 merged) |
| Argo | OutOfSync / Healthy |
| ConfigMaps | `openedx-settings-*-patched` (governed by configMapGenerator) |
| CMS JWT pub==priv | TRUE |
| LMS enterprise catalog | http://enterprise-catalog:8160 |

## Prod Parked Starting State

| Workload | Replicas | Intent |
|----------|----------|--------|
| LMS, CMS, MFE, MySQL, Redis, etc. | 0 | Cost-parked |
| Caddy, preview-redirect | 1 | Maintenance page |
| payments-gateway | 2-3 | Active payment processing |
| postgresql-payments | 1 | Payments database |

Verification: `verify-prod-parked-state.sh` → 15/15 PASS

## Repo Changes

- PR #926 merged (JWT key derivation — already done in Lane D1)
- No new repo changes needed this lane

## Infra Changes

- PR #1824 merged (vendor-sync base files — already done in Lane D1)
- PR #1825 merged (overlay staging fixes — already done in Lane D1)
- No new infra changes needed this lane

## PR #926 Status: MERGED

## PR #924 Vendor-Synced: YES (PR #1824 + #1825)

## Patched ConfigMaps: GOVERNED

The `openedx-settings-*-patched` ConfigMaps are generated by
kustomize `configMapGenerator` with `disableNameSuffixHash: true`.
They are governed, not manually created.

## Argo Before/After

- Before: OutOfSync / Healthy
- After: OutOfSync / Healthy (non-governing diffs remain: stale
  resources not pruned due to Prune=false)

## Prod Parked Semantics

- Runbook created: `docs/ops/runbooks/PROD_PARKED_MODE.md`
- Verification script: `scripts/qa/verify-prod-parked-state.sh`
- 15/15 checks pass

## Prod Scale-Up Proof

**BLOCKED.** First exact blocker:
```
RuntimeError: Missing required environment variable: ECOMMERCE_API_SIGNING_KEY
```
The `openedx-secrets` secret in prod namespace is empty or missing
required keys. ExternalSecrets may not have synced. Prod was returned
to parked state after identifying this blocker.

## Fresh Active-Env Runtime Proof

| Surface | Method | Result |
|---------|--------|--------|
| /authn/login | curl | HTTP 200 |
| /courses API | curl | 109 courses |
| /courses page | curl | HTTP 200 |
| Course home API | curl | HTTP 200 |
| Studio API (unauth) | curl | 401 (expected) |
| Studio /authoring/home | browser | Renders: "Studio home", "New course", "New library" |
| Dashboard | curl | 302 → login (expected) |
| MFE config | curl | LMS + Studio URLs correct |
| CronJob | kubectl | Present (30 */6 * * *) |

## DR Proof Result

**BLOCKED** by missing restore namespace (`velero-restore-test`).
Script works correctly — needs a restore drill to have been run first.

## Observability Verifier Result

**PASS.** Required checks: PASS. Recommended checks: PASS.

## Remaining Blockers

| Blocker | Owner | Severity |
|---------|-------|----------|
| Prod secrets empty (ECOMMERCE_API_SIGNING_KEY etc.) | Secrets/infra owner | Blocks prod wake-up |
| DR restore drill not run | Ops owner | Blocks DR proof only |
| Argo OutOfSync (non-governing) | Platform owner | Cosmetic |
| Studio URL config mismatch | Config owner | Cosmetic |
