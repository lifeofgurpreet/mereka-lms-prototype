# Lane D1 — Durability Closeout

Date: 2026-03-15T20:40Z

## Starting State

| Item | Value |
|------|-------|
| LMS/CMS ConfigMap source | `openedx-settings-*-patched` (manually created) |
| Argo sync | OutOfSync / Healthy |
| JWT key derivation | Live-only (patched CM), not in repo or infra |
| Enterprise catalog URL | Merged in app repo (#924), not in infra |
| Live surfaces | All working |

## Drift Found

1. CMS `JWT_PUBLIC_SIGNING_JWK_SET` had stale hardcoded key in both
   repo base files AND infra overlay staging files
2. `ENTERPRISE_CATALOG_INTERNAL_ROOT_URL` missing from infra overlay
   staging LMS file
3. `openedx-settings-*-patched` ConfigMaps were manually created
   but the overlay `configMapGenerator` also produces them — the
   manual versions shadowed the governed versions

## Changes Made

### App Repo (mereka-lms)
- PR #926 (merged): JWT public key derivation in LMS + CMS base
  `production.py`

### Infra Repo (bbi-infrastructure)
- PR #1824 (merged): Vendor-sync base `production.py` files
- PR #1825 (merged): Fix overlay staging files
  - CMS: JWT derivation replaces hardcoded stale key
  - LMS: add `ENTERPRISE_CATALOG_INTERNAL_ROOT_URL`

### Live Cluster
- Rolled LMS + CMS pods to pick up governed ConfigMap content
- No manual ConfigMap patches remain — `openedx-settings-*-patched`
  are now generated by kustomize `configMapGenerator`

## Proof After Reconciliation

| Surface | Result |
|---------|--------|
| `/authn/login` | HTTP 200 |
| `/courses` API | 109 courses |
| Course home API | HTTP 200 |
| Studio API (unauth) | 401 (expected) |
| CMS JWT pub==priv | True |
| LMS JWT pub==priv | True |
| LMS enterprise catalog URL | http://enterprise-catalog:8160 |
| Course-reindex CronJob | Present (30 */6 * * *) |
| Argo | OutOfSync / Healthy (non-governing diffs remain) |

## Final Durability Classification

| Fix | Classification |
|-----|---------------|
| JWT public key derivation (LMS) | LIVE_AND_GOVERNED |
| JWT public key derivation (CMS) | LIVE_AND_GOVERNED |
| ENTERPRISE_CATALOG_INTERNAL_ROOT_URL | LIVE_AND_GOVERNED |
| MFE authn parse fix | LIVE_AND_GOVERNED |
| Course-reindex CronJob | LIVE_AND_GOVERNED |
| Studio URL config (/course-authoring vs /authoring) | KNOWN_RED |
| Argo OutOfSync (non-governing diffs) | KNOWN_RED |

## Remaining Reds

1. **Studio URL config mismatch** — MFE config advertises
   `/course-authoring` but MFE builds with basename `/authoring`.
   Studio works at `/authoring/home`. Cosmetic only.
2. **Argo OutOfSync** — non-governing diffs remain (stale resources
   not pruned due to `Prune=false`). Not a product or governance issue.
