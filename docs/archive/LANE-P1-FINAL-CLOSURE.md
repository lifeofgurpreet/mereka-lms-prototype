# Lane P1 — Final Closure

Date: 2026-03-15T21:50Z

## 1. Active-Env Starting State

- Argo: OutOfSync / Healthy (6 orphaned ConfigMaps)
- ConfigMaps: governed (configMapGenerator)
- Runtime: JWT derivation + enterprise catalog fixes live and correct
- Manual rollout restart was required for ConfigMap content changes

## 2. Prod Parked Starting State

- Core workloads at 0 replicas (intentional)
- Caddy, payments-gateway, preview-redirect running
- ArgoCD parked (0 replicas)
- Parked-state verifier: 15/15 PASS

## 3. Repo Changes

- PR #926 already merged (JWT key derivation) — no new repo changes

## 4. Infra Changes

- PR #1827 merged: ENTERPRISE_CATALOG_INTERNAL_ROOT_URL in prod overlay
- PR #1828 merged: ECOMMERCE_API_SIGNING_KEY made optional in prod
- 6 orphaned ConfigMaps deleted from dev namespace

## 5. PR #926: MERGED (prior lane)

## 6. PR #924 Vendor-Synced: YES (PRs #1824 + #1825, prior lane)

## 7. Patched ConfigMaps: GOVERNED

The `openedx-settings-*-patched` ConfigMaps in dev are generated by
kustomize configMapGenerator. They are governed, not manual.

Note: ConfigMap content changes with `disableNameSuffixHash: true`
require manual `kubectl rollout restart` since the ConfigMap name
doesn't change. Prod overlay uses hash suffixes, so prod gets
automatic rollouts.

## 8. Argo Before/After

- Dev before: OutOfSync / Healthy
- Dev after: **Synced / Healthy**
- Prod: ArgoCD parked (0 replicas) — not applicable

## 9. Prod Parked Semantics

Runbook: `docs/ops/runbooks/PROD_PARKED_MODE.md`
Verifier: `scripts/qa/verify-prod-parked-state.sh` → 15/15 PASS

## 10. Prod Scale-Up Proof

**BLOCKED.** Two blockers found in sequence:

1. ECOMMERCE_API_SIGNING_KEY empty → **FIXED** (PR #1828)
2. `ModuleNotFoundError: No module named 'openedx_prometheus'`
   → prod image (`openedx:8a7f2476`) predates the addition of
   the `openedx_prometheus` custom app. **Not fixable without
   rebuilding the prod image or guarding the import.**

Prod was returned to parked state after identifying blocker 2.

## 11. Fresh Active-Env Runtime Proof

| Surface | Result |
|---------|--------|
| /authn/login | HTTP 200 |
| /courses API | 109 courses |
| /courses page | HTTP 200 |
| Course home API | HTTP 200 |
| Studio /authoring/home | Renders (browser-proven) |
| Dashboard | 302 → login (expected) |
| Argo | Synced / Healthy |
| CronJob | Present |

## 12. DR Proof

BLOCKED: restore namespace `velero-restore-test` doesn't exist.
Needs an actual restore drill to be run first.

## 13. Observability Verifier

PASS. Required + recommended checks pass.

## 14. Remaining Blockers

| Blocker | Owner | Severity |
|---------|-------|----------|
| Prod image lacks openedx_prometheus | Image build owner | Blocks prod wake-up |
| DR restore drill not run | Ops owner | Blocks DR proof |
| Studio URL config cosmetic | Config owner | Non-blocking |

## Durability Classification

| Fix | Status |
|-----|--------|
| JWT public key derivation | LIVE_AND_GOVERNED |
| Enterprise catalog internal URL | LIVE_AND_GOVERNED |
| MFE authn parse fix | LIVE_AND_GOVERNED |
| Course-reindex CronJob | LIVE_AND_GOVERNED |
| Prod parked-mode semantics | LIVE_AND_GOVERNED |
| ECOMMERCE_API_SIGNING_KEY optional | LIVE_AND_GOVERNED |
| Prod enterprise catalog URL | LIVE_AND_GOVERNED |
| Dev Argo sync | LIVE_AND_GOVERNED (Synced/Healthy) |
| Prod image parity | BLOCKED |
| DR restore drill | BLOCKED |
