# RKE2 Nonprod LMS ArgoCD blocker evidence

Date: 2026-02-19T13:50:00Z

## Commands run

1) List contexts
```bash
kubectl config get-contexts
```
Output: `rke2-nonprod` is current.

2) Argo apps in rke2-nonprod
```bash
kubectl --context rke2-nonprod get app -n argocd | rg -i "mereka|lms|rke2"
```
Output includes:
- `mereka-lms-local` (env=local)
- NO namespace-specific `staging` LMS app
- many other apps healthy/unknown

3) ExternalSecrets in `mereka-lms`
```bash
kubectl --context rke2-nonprod get externalsecret -n mereka-lms
```
Output: `No resources found in mereka-lms namespace.`

4) Pods in `mereka-lms`
```bash
kubectl --context rke2-nonprod get pods -n mereka-lms
```
Output: `No resources found in mereka-lms namespace.`

5) Describe LMS app
```bash
kubectl --context rke2-nonprod describe app -n argocd mereka-lms-local | sed -n '1,120p'
```
Error condition observed:
`ComparisonError` message:
- `Failed to load target state: failed to compare revisions for source 1 of 1: rpc error: code = Internal desc = unable to resolve git revision : failed to list refs: NOAUTH Authentication required.`

## Conclusion
RKE2 migration is blocked at Git auth for ArgoCD source repository before any LMS secrets/pods can be created.

## Next required action
- Fix ArgoCD repository secret / credentials for `https://github.com/Biji-Biji-Initiative/bbi-infrastructure.git` and ensure repo access for `main`.
- Re-evaluate after auth is fixed: external-secrets should be applied and pods should materialize.
