{
  "name": "mereka-lms-spa",
  "version": "0.1.0-phase-0",
  "description": "Mereka LMS — SPA frontend talking to Open edX backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "sass-embedded": "^1.77.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "oauth4webapi": "^2.17.0"
  }
}
