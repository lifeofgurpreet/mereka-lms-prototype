# Legacy Testmaps Retirement Plan

`specs/testmaps/**` is frozen legacy compatibility.

## Current contract

- active generated verification mapping lives in `specs/_generated/testmaps/**`
- edits under `specs/testmaps/**` are blocked by CI

## Retirement trigger

Retire the legacy path when remaining references are updated and the frozen surface is no longer needed for compatibility.
