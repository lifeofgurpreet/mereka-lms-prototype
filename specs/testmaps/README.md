# Legacy Testmaps

_Status: frozen legacy compatibility surface_

This directory is retained only for compatibility with older readers and historical references.

- Active generated testmaps live in `specs/_generated/testmaps/**`.
- Do not add new files here.
- Do not edit the legacy `*.testmap.yml` files here.
- CI blocks changes under `specs/testmaps/**` except this README.

If you need the current generated verification map for a spec, use the matching file under:

- `specs/_generated/testmaps/`
