# Declarative Enterprise Tenant Bootstrap Spec
#
# This file defines the desired state for all enterprise tenants
# in the dev environment. It is consumed by:
#   - scripts/tenants/bootstrap-enterprise-tenants.py (bootstrap tool)
#   - scripts/tenants/validate-enterprise-tenants.py (validation tool)
#
# Default behavior: DRY RUN. Use --apply to mutate data.
#
# Idempotent: running twice produces no duplicate records.
---
version: "1.1"
environment: dev
lms_domain: academyv2.mereka.dev

standalone_users:
  - username: lanea-platform-admin
    email: lanea-platform-admin@synthetic.test
    is_staff: true
    is_superuser: true
  - username: lanea-unlinked-user
    email: lanea-unlinked-user@synthetic.test
    is_staff: false
    is_superuser: false

# Global waffle flags (not per-tenant — apply once per environment)
waffle_flags:
  learner_home_mfe.enabled: true  # Redirect /dashboard to MFE learner-home

tenants:
  - slug: mereka
    name: Mereka Academy
    org_code: MEREKA
    active: true
    contact_email: team@mereka.io
    country: MY
    is_platform_operator: true

    site:
      domain: academyv2.mereka.dev

    enterprise_customer:
      # UUID is generated on first create, then becomes stable.
      # If a UUID is provided, the tool uses it for get_or_create.
      # uuid: null  # auto-generated
      enable_data_sharing_consent: true
      enforce_data_sharing_consent: externally_managed
      enable_audit_enrollment: false
      enable_learner_portal: true
      enable_portal_code_management: false
      enable_analytics_screen: true

    catalogs:
      - title: "Mereka Academy Full Catalog"
        catalog_query:
          content_filter:
            content_type: course
            aggregation_key: ["courserun:*"]
          # All MEREKA org courses
          org_filter:
            - MEREKA

    user_links:
      # Link admin users to this enterprise customer.
      # email is the lookup key. role is "admin" or "learner".
      - email: team@mereka.io
        role: admin
      # Canonical test users (smoke-account-registry.yaml)
      - email: lanea-enterprise-learner@synthetic.test
        role: learner
      - email: lanea-enterprise-admin@synthetic.test
        role: admin
      # Single-tenant automation user (smoke-account-registry.yaml: mereka_learner)
      - email: lanea-mereka-learner@synthetic.test
        role: learner

    waffle_switches:
      enterprise.enable_learner_portal: true
      enterprise.enable_analytics_screen: true
      enterprise.enable_audit_enrollment: false
      enterprise.enable_portal_code_management: false
      enterprise.enable_integrated_learner_portal_search: true

  - slug: biji-biji
    name: Biji-Biji Academy
    org_code: BIJIBIJI
    active: true
    contact_email: admin@biji-biji.com
    country: MY
    is_platform_operator: false

    site:
      domain: biji-biji.academyv2.mereka.dev

    enterprise_customer:
      enable_data_sharing_consent: true
      enforce_data_sharing_consent: externally_managed
      enable_audit_enrollment: false
      enable_learner_portal: true
      enable_portal_code_management: false
      enable_analytics_screen: true

    catalogs:
      - title: "Biji-Biji Academy Catalog"
        catalog_query:
          content_filter:
            content_type: course
            aggregation_key: ["courserun:*"]
          org_filter:
            - BIJIBIJI
            # NOTE: If biji-biji should also see MEREKA courses,
            # add MEREKA to this list. Decision pending.

    user_links:
      - email: admin@biji-biji.com
        role: admin
      - email: lanea-enterprise-learner@synthetic.test
        role: learner
      - email: lanea-enterprise-admin@synthetic.test
        role: admin
      # Single-tenant automation user (smoke-account-registry.yaml: bb_learner)
      - email: lanea-bb-learner@synthetic.test
        role: learner

    waffle_switches:
      enterprise.enable_learner_portal: true
      enterprise.enable_analytics_screen: true
      enterprise.enable_audit_enrollment: false
      enterprise.enable_portal_code_management: false
      enterprise.enable_integrated_learner_portal_search: true

  - slug: skillourfuture
    name: Skill Our Future
    org_code: SKILLOURFUTURE
    active: true
    contact_email: admin@mereka.io
    country: MY
    is_platform_operator: false

    site:
      domain: skillourfuture.academyv2.mereka.dev

    enterprise_customer:
      enable_data_sharing_consent: true
      enforce_data_sharing_consent: externally_managed
      enable_audit_enrollment: false
      enable_learner_portal: true
      enable_portal_code_management: false
      enable_analytics_screen: true

    catalogs:
      - title: "Skill Our Future Catalog"
        catalog_query:
          content_filter:
            content_type: course
            aggregation_key: ["courserun:*"]
          org_filter:
            - SKILLOURFUTURE
            # NOTE: If SOF should also see MEREKA courses,
            # add MEREKA to this list. Decision pending.

    user_links:
      - email: admin@mereka.io
        role: admin
      - email: lanea-enterprise-learner@synthetic.test
        role: learner
      - email: lanea-enterprise-admin@synthetic.test
        role: admin
      # Single-tenant automation user (smoke-account-registry.yaml: sof_learner)
      - email: lanea-sof-learner@synthetic.test
        role: learner

    waffle_switches:
      enterprise.enable_learner_portal: true
      enterprise.enable_analytics_screen: true
      enterprise.enable_audit_enrollment: false
      enterprise.enable_portal_code_management: false
      enterprise.enable_integrated_learner_portal_search: true

# Decisions requiring human input before bootstrap --apply:
#
# 1. Should biji-biji and skillourfuture catalogs include MEREKA org courses?
#    Currently they only include their own org. If those orgs have zero courses,
#    the catalogs will be empty.
#
# 2. UUIDs: ECs already exist in dev DB with stable UUIDs:
#    - mereka:         caee80bc-2e05-4b92-8ebe-d0ec045edaeb
#    - biji-biji:      7ad11569-d027-4e9d-a08f-e65c57e27c8b
#    - skillourfuture: ef9e4eff-4c5d-464b-a650-2000bad56c99
#    bootstrap-enterprise-tenants.py looks up by slug, so existing ECs are reused.
#
# 3. Admin users: gurpreet@biji-biji.com has enterprise_admin on all 3 ECs.
#    The email addresses below are for bootstrap user_links (ECU records).
