# GENERATED — do not hand-edit. Source: deploy/k8s/tenancy/tenant-registry.yaml
# Regenerate: python scripts/domains/generate_domain_env.py
# Patch Caddy Deployment env vars for academyv2.mereka.io domain.
# The Caddyfile uses {$VAR} placeholders; these env vars set the actual hostnames.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: caddy
spec:
  template:
    spec:
      containers:
        - name: caddy
          env:
            - name: LMS_HOST
              value: "academyv2.mereka.io"
            - name: LMS_HOST_PREVIEW
              value: "preview.academyv2.mereka.io"
            - name: STUDIO_HOST
              value: "studio.academyv2.mereka.io"
            - name: MFE_HOST
              value: "apps.academyv2.mereka.io"
            - name: DISCOVERY_HOST
              value: "discovery.academyv2.mereka.io"
            - name: NOTES_HOST
              value: "notes.academyv2.mereka.io"
            - name: CREDENTIALS_HOST
              value: "credentials.academyv2.mereka.io"
            - name: ENTERPRISE_ADMIN_HOST
              value: "admin.academyv2.mereka.io"
            - name: ENTERPRISE_LEARNER_HOST
              value: "learner.academyv2.mereka.io"
