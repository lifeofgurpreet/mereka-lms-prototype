project_id  = "mereka-lms"
region      = "asia-southeast1"
zone        = "asia-southeast1-a"
domain_root = "staging.academy.mereka.io"

# Generate a strong password and inject via environment variable or secret manager.
cloudsql_root_password = "CHANGE_ME"
cloudsql_root_username = "root"
cloudsql_tier          = "db-custom-2-7680"
cloudsql_disk_size_gb  = 100
redis_memory_size_gb   = 2

# Example Secret Manager entries (remove or adjust before applying)
# secrets = [
#   {
#     name = "lms-django-secret"
#     data = "REDACTED"
#   }
# ]
